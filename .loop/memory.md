# Project Memory — loop architecture

Read on demand by `/council`, `/council-deep`, and `/ui-refine` when they run in **this** project, and
**auto-appended** by them via `scripts/loop-memory.mjs` (append-only, timestamped, provenance-tagged,
auto-compacted to the last ~25 entries per section). Newest entries sit at the top of each section. A line
marked `[superseded]` was overridden by a later one. Structured machine state lives alongside this in
`.loop/state.json` (including a `next` target naming the question to research / flow to verify / diff to review,
and a `budget` the metabolism governor reads + writes).

Format: `- [YYYY-MM-DD · organ] summary <!-- id -->`

## Decisions
<!-- Settled calls + the why. Auto-written by /council & /council-deep verdicts. -->

## Standards
- [2026-06-24 · ui-refine] Board UI = checkerboard: two cream tones (--board-light 45 33% 97% / --board-dark 40 26% 87%), uniform squares via grid-template-columns+rows repeat(N,minmax(0,1fr)), per-cell highlights as absolute z-0 overlays under z-10 content so the checker texture shows through. <!-- 910a4f64 -->
- [2026-06-23 · ui-refine] Reference exemplar = design.md + design-reference.jpeg (neo-brutalist tactical-arcade). Stack: React+Vite+TS+Tailwind+shadcn; board is pure DOM CSS-grid (no WebGL, skip A-3D gate) <!-- ffd39e62 -->
<!-- The bar to hold (reference exemplar, stack, targets). Auto-written by /ui-refine. -->

## Lessons
- [2026-06-25 · ui-refine] CameraFit zoom margin (2026-06-25): divisor of 1.12 leaves 12% bone background visible inside CSS container border — board looks small/floating in the frame. Use 1.03 for 3% safety margin (97% fill). Formula: zoom = min(canvas_w, canvas_h) / (BOARD_SIZE * 1.03). Sufficient for edge-cell tanks to stay in view. <!-- fe0d43fa -->
- [2026-06-25 · ui-refine] Orthographic camera (2026-06-25): perspective camera at [0,22,0.001] fov=55 produced visible trapezoidal distortion — far cells appeared smaller than near cells. Fix: Canvas orthographic + CameraFit component (useEffect on camera+size, zoom = min(w,h)/(BOARD_SIZE*1.12)) makes board fill ~89% of canvas with zero perspective distortion. Use orthographic for top-down board games; perspective is only appropriate when vertical relief matters. <!-- 4de03914 -->
- [2026-06-25 · reflect] WHEN visual inspection of the board or 3D scene is needed during a ui-refine session -> push branch to GitHub, run Playwright in CI with --use-angle=swiftshader --enable-unsafe-swiftshader, upload artifact, pull with gh run download, Read the PNG — never run local Puppeteer or headless Chrome for WebGL pages (because kit CLAUDE.md Loop B explicit instruction; local headless cannot render WebGL without SwiftShader; local Puppeteer timed out and failed this session; CI runner has GPU emulation) <!-- e6f7aad0 -->
- [2026-06-25 · ui-refine] Board design v2 (2026-06-25): removed thick 3D platform box entirely; replaced with in-texture perimeter border (72% ink strokeRect). Tactical grid: #FEFCF8/#E8E2D6 checker (high contrast), 18% minor lines every cell, 40% major lines every 5 cells (quadrant dividers), roughness 0.55 for subtle sheen. Added token base discs to TankMesh: colored circle (42% opacity) + ink ring (68%) directly on the board surface — mirrors DOM TankToken circular-bordered style. Scene bg changed to bone #F5F2EA to match app surface. Cell px upped 26→30 for crispness. <!-- f0a30295 -->
- [2026-06-25 · ui-refine] Board env removal (2026-06-25): stripped Minecraft biome terrain (4-biome texture, grass plane, 16 trees, sky gradient, fog) and replaced with clean neo-brutalist surface (bone/paper checker + 10% ink grid, ink platform frame, sand canvas bg). Camera locked top-down via position [0,22,0.001] fov 55 + OrbitControls enableRotate=false enableZoom=false (component kept). Code-split three/R3F into lazy chunk (925KB) via manualChunks + React.lazy on BoardScene3D; reduced TBT 684→464ms. <!-- 70651ab2 -->
- [2026-06-25 · ui-refine] TBT budget (200ms in lighthouserc.json) was silently passing on stale dist/ builds that predated the 3D board. First real build with Three.js reveals structural ~800ms TBT. The correct gate for WebGL game pages is audit:3d (procedural geometry) + SIGNOFF.md human checks. The DOM-shell perf budget needs a separate investigation (OffscreenCanvas/Worker) if TBT must pass; it cannot be fixed without removing or worker-izing the canvas. <!-- 5693598a -->
- [2026-06-24 · ui-refine] 3D battlefield terrain (Stage 3): replaced the placeholder cream checker with a dusty khaki WAR-TORN checker (CanvasTexture = checker + grime speckles + scorch radial-gradients + cracks), set the playable grid as a raised dark PLATFORM inside a larger tiling-dirt battlefield plane, scattered craters (dark disc + scorch ring) + rubble boxes OFF the grid, and swapped the flat lavender bg for a dusk gradient (scene.background via useThree) + warm low-angle key light (long shadows) + dim cool ambient + dust fog. Static geometry screenshots fine (unlike RAF-animated FX); cells stay readable because the checker contrast survives the grime. <!-- e88c9093 -->
- [2026-06-24 · ui-refine] 3D tank-shot polish: aim the turret by rotating a turret sub-group to atan2(dx,dz) (dt-lerped) so the barrel tracks the target, and originate the muzzle flash/tracer from the barrel tip; render the lock-on reticle ABOVE the cell with depthTest:false + high renderOrder so a targeted tank never occludes it; flash pointLights at muzzle + impact give the biggest perceived 'punch' per effort in 3D. <!-- 56310ac5 -->
- [2026-06-24 · ui-refine] Headless screenshots CANNOT capture R3F sub-second animations: the RAF loop throttles/freezes in headless Chrome (even with --disable-background-timer-throttling/--disable-renderer-backgrounding + bringToFront) after the first frames, so the scene freezes mid-FX (6 burst frames came back pixel-identical). Verify timed 3D FX (muzzle/tracer/impact) LIVE; static states (turret aim, floating reticle, tile highlights) DO capture fine. <!-- 67d1fccd -->
- [2026-06-24 · ui-refine] 3D board interactivity (R3F/three): cell-picking via raycast on the ground plane (e.point -> round(x+HALF) to cell); the control bar stays DOM (accessible) and the scene reads shared React state; tanks lerp toward their target cell in useFrame for smooth glides through a stacked chain; shot FX = useFrame-driven projectile + muzzle + impact ring. Move-stacking/reticle/resolve ported 1:1 from the DOM board. CAVEAT: cell-picking is mouse-only on the canvas - keyboard/AT fallback still required (Stage 4), and the three.js bundle (+~900KB) needs code-splitting before the DOM-shell perf gate can pass. <!-- 93876d44 -->
- [2026-06-24 · ui-refine] Demo move-stacking: plannedMoves[] chain, each step extends from the projected end (neighbors of the last cell, excluding the path + other tanks), capped by the prime AP ladder (1,2,3,5,7 - 7 AP allows 3 moves). Rendered as a dashed SVG polyline + violet waypoint DOTS (shapes, not numbers - dodges the tiny-text axe trap) + a ghost at the chain end. Step-through resolve re-mounts the token per hop so the stamp replays X->Y->Z. <!-- 722fc234 -->
- [2026-06-24 · ui-refine] Coordinate labels at text-[8px] inside cells add ~21 axe tiny-glyph contrast violations even when aria-hidden. Board coord labels belong in full-size edge gutters, not tiny inside cells. <!-- 5d49471a -->
- [2026-06-24 · ui-refine] Solid checkerboard cell backgrounds FIXED the long-standing tank-token color-contrast failures (7 -> 0): axe couldn't read the token's effective bg over transparent/border-only cells; a solid cell bg lets it compute contrast. A production fix that also closed the a11y gap - confirms lesson #1 (fix why axe can't read the bg, don't chase the ratio). <!-- 6a6424f9 -->
- [2026-06-24 · ui-refine] lighthouserc.json was pointed at the DEV server (:5173) -> unbundled-ESM false LCP/FCP fails. Fixed: startServerCommand 'npm run preview' + url :4173 (prod build) -> perf 96-99. Encodes the prod-vs-dev perf lesson into the gate config so it stops false-failing. <!-- 4eadde49 -->
- [2026-06-24 · ui-refine] Puppeteer-driving the /game board: ActionQueuePanel renders its own Move/Shoot buttons earlier in the DOM, so a plain text match clicks the wrong one - scope board-control clicks to the parent bar of the unique Resolve button <!-- d7ef9691 -->
- [2026-06-24 · ui-refine] Board-feedback animation pattern: queued-move shadow ghost + dashed connector, queued-shot coral reticle, and tank-shot tracer/muzzle/impact+recoil built as DOM/SVG over the CSS-grid board. Overlay <svg viewBox='0 0 N N'> maps 1 unit = 1 cell; ghost/reticle render in-cell; keyframes live in index.css and are all disabled under prefers-reduced-motion <!-- 3a05ff99 -->
- [2026-06-23 · ui-refine] axe color-contrast flags text that overflows its box or tiny text glyphs even at 12:1 (the 04:12 chip clipped a fixed-square; the text arrow). Fix the layout (auto-width) or swap the glyph for an SVG icon -- don't chase the ratio, find why axe can't read the bg <!-- 12b722bc -->
- [2026-06-23 · ui-refine] Brand violet --primary #8B5CF6 fails WCAG AA for small text (white-on-violet 4.32:1, ink-on-violet 4.35:1, need 4.5). Darkening helps white but hurts ink text -> it is a host design-token decision, do not silently change <!-- ef8c7d8a -->
- [2026-06-23 · ui-refine] Windows: lhci/chrome-launcher crashes cleaning the temp Chrome profile (EBUSY unlink). Workaround: run lighthouse with --chrome-flags=--user-data-dir=<persistent dir>, or run the perf gate in CI (Linux) <!-- 265889cb -->
- [2026-06-23 · ui-refine] Measure perf against the PROD build (vite preview :4173), not the Vite dev server: dev serves unbundled ESM so FCP/LCP run ~2-3s and fail budgets, while the prod build scores 99/100 <!-- 4d040307 -->
<!-- What bit us, so we don't repeat it. Auto-written by /ui-refine, /immune, /eyes. -->
